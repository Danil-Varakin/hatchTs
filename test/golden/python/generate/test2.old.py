# [PYDUP] одинаковый метод в трёх классах, правка в СРЕДНЕМ
class Alpha:
    def size(self):
        return len(self.items)


class Beta:
    def size(self):
        return len(self.items)


class Gamma:
    def size(self):
        return len(self.items)
