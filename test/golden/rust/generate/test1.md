# match rs
    ...
    pub fn longest<
    ...
    >(
    ...
    ) -> &'a str {
    ...
    >>>
        if left.len() > right.len() { left } else { right }
    <<<
    ...
    }
    ...
# end
# patch
    if left.len() > right.len() { right } else { left }
# end
