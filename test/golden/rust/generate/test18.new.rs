// [DEEPCLOSURE] восемь вложенных замыканий, правка на дне (стек блоков шаблона)
pub fn nest() -> u32 {
    run(|| {
        run(|| {
            run(|| {
                run(|| {
                    run(|| {
                        run(|| {
                            run(|| 7)
                        })
                    })
                })
            })
        })
    })
}
