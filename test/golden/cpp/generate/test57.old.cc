// [STRUCT] KNOWN-GAP: два ИДЕНТИЧНЫХ if-блока в одной функции, правка во втором.
// Синтез НЕ МОЖЕТ это привязать, и это записано намеренно: различить блоки способен
// только текст ПЕРВОГО из них, а добор соседей ограничен ближайшим родителем
// (synth.neighbourStepsAbove: limit = parents[0].open + 1). Красный и при --siblings 16.
// Когда почините — уберите маркер, тест сам об этом скажет. BACKLOG.md §1.
void f() {
  if (cond) {
    a();
    b();
    c();
    work();
  }
  if (cond) {
    a();
    b();
    c();
    work();
  }
}
